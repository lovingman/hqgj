<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.huacainfo.ace.zcpa.dao.TimerDao">

    <select id="findEndProjectId" resultType="java.lang.String">
        select t.id from  act_project t where  DATE_FORMAT(t.endTime,'%Y-%m-%d') <![CDATA[ < ]]> DATE_FORMAT(NOW(),'%Y-%m-%d') and t.projectState !='4'
    </select>


    <update id="batchUpdate" parameterType="java.lang.String">
        update act_project
        set   projectState = '4'
        where  id  in
        <foreach collection="list" item="ids" open="(" separator="," close=")">
            #{ids}
        </foreach>
    </update>


</mapper>